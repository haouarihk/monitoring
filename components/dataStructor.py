class DataStructor:
    def __init__(self, url, oldData, newData, timeOfChange):
        self.url = url
        self.oldData = oldData
        self.newData = newData
        self.timeOfChange = timeOfChange

